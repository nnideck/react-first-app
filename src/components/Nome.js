function Nome(props){
  return(
    <span>
usuario: {props.aluno} tem {props.idade} anos    
    </span>
  )
}

/* function Nome({ aluno} ){
  return(
    <span>
usuario: {aluno}  
    </span>
  )
} */

export default Nome;
